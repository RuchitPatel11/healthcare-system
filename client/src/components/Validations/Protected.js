// import { Navigate } from 'react-router-dom';

// function Protected({ condition, children, redirectTo = '/' }) {
//   return condition ? { ...children } : <Navigate to={redirectTo} />;
// }
// export default Protected;